type Mutation {
  submitCroInterest(
    email: String!
    company_name: String!
    company_type: String!
    service: String!
    interest: String!
    first_name: String!
    last_name: String!
  ): Boolean
  subscribeEmailUpdates(
    email: String!
  ): Boolean
}
