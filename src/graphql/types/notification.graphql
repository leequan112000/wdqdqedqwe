type Notification {
  id: String
  notification_type: String
  params: JSON
  read_at: Date
  created_at: Date
  updated_at: Date
  user_id: String
  user: User
}

type Query {
  notifications(unread_only: Boolean): [Notification]
}
